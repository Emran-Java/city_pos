<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/llEmiRootDetails"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="0dp">
    <!-- Title -->
    <TextView
        android:id="@+id/tvEmiTranTypeTitle"
        style="@style/RepotMerchantDetailsTextAppearance"
        android:layout_width="match_parent"
        android:gravity="center"
        android:paddingHorizontal="10dp"
        android:paddingVertical="8dp"
        android:text="@string/core_payflex_details_report"
        android:textAllCaps="true"
        android:textSize="16sp"
        android:textStyle="bold" />

    <include
        android:id="@+id/incEmiMerchant"
        layout="@layout/ll_emi_merchant_info" />

    <!--Show Records-->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvEmiTranType"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:nestedScrollingEnabled="false"
        tools:itemCount="1"
        tools:listitem="@layout/item_card_report" />

    <!-- Card report -->
    <TextView
        android:id="@+id/tvEmiCardReportTitle"
        style="@style/RepotMerchantDetailsTextAppearance"
        android:layout_width="match_parent"
        android:layout_marginTop="8dp"
        android:background="@drawable/item_card_details_table_row_bg"
        android:gravity="center"
        android:padding="8dp"
        android:text="Card Report"
        android:textSize="16sp"
        android:textStyle="bold" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvEmiCardScheme"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:nestedScrollingEnabled="false"
        tools:itemCount="1"
        tools:listitem="@layout/item_card_scheme_report" />


</LinearLayout>